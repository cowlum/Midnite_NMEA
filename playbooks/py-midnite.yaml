### Install Python modules
### Install .py files and config
### Setup Systemd 

- hosts: all
  remote_user: pi
  become: yes
  gather_facts: false
  vars:    

  tasks:

    #- name: Intall pip required packages
      #pip: name=pynmea2,pyserial,configparser,bme280,smbus2
      #  executable: pip-3.3
      #with_items: python_packages_to_install
# Install (Bottle) python package.
    - pip:
        name: pynmea2,pyserial,configparser,RPi.bme280,smbus2
        executable: pip3

    


